---
# ┌──────────────────────────────────────────────────────────────┐
# │  Server Inventory                                            │
# │  Ganti IP_VPS_KAMU dengan IP address VPS kamu               │
# └──────────────────────────────────────────────────────────────┘
all:
  hosts:
    smart-vps:
      ansible_host: 203.194.114.62        # ← Ganti dengan IP VPS
      ansible_port: 22
      ansible_user: deploy             # ← User SSH (dibuat oleh setup-server.yml)
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519

  children:
    production:
      hosts:
        smart-vps:
